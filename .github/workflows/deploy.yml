name: Deploy to Render

on:
  push:
    branches:
      - main

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create GitHub deployment
        id: create_deployment
        run: |
          response=$(curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/deployments \
            -d '{
              "ref": "main",
              "environment": "production",
              "auto_merge": false,
              "required_contexts": []
            }')

          deployment_id=$(echo "$response" | jq -r '.id')
          echo "DEPLOYMENT_ID=$deployment_id" >> $GITHUB_ENV
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Trigger Render deployment
        run: |
          response=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys)

          deploy_id=$(echo "$response" | jq -r '.id')
          echo "RENDER_DEPLOY_ID=$deploy_id" >> $GITHUB_ENV

      - name: Wait for Render deployment
        run: |
          status="pending"
          while [ "$status" = "pending" ] || [ "$status" = "started" ]; do
            sleep 10
            status=$(curl -s \
              -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
              https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys/$RENDER_DEPLOY_ID \
              | jq -r '.status')
            echo "Render status: $status"
          done

          echo "RENDER_STATUS=$status" >> $GITHUB_ENV

      - name: Update GitHub deployment status
        run: |
          state="failure"
          if [ "$RENDER_STATUS" = "deployed" ]; then
            state="success"
          fi

          curl -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/deployments/$DEPLOYMENT_ID/statuses \
            -d "{
              \"state\": \"$state\",
              \"environment_url\": \"https://dashboard.render.com/web/${{ secrets.RENDER_SERVICE_ID }}/deploys/$RENDER_DEPLOY_ID\"
            }"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
